spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://127.0.0.1:3306/menu?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&useUnicode=true}
    username: ${DB_USER:menu}
    password: ${DB_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver


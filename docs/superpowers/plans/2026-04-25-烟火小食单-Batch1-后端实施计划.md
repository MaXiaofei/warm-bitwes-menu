# 烟火小食单 Batch1 后端 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `menu` 后端仓库内补齐 Batch1 的真实鉴权、角色权限、端权限范围，以及管理后台所需的菜品/食材/权限 API，保证管理后台与小程序能基于统一返回体完成联调。

**Architecture:** 保持现有 Spring Boot 多模块结构（`controller` / `service` / `mapper` / `common` / `boot`），在现有 `AuthController`、`DishController`、`IngredientController` 基础上增量扩展。权限模型以后端 API 权限点为准，前端只做路由/按钮映射；请求鉴权统一走拦截器/上下文，异常继续通过全局异常处理输出 `code/message/data`。

**Tech Stack:** Java 17, Spring Boot, MyBatis, MySQL, Jakarta Validation, JUnit 5

---

## File Structure

- Modify: `controller/src/main/java/com/warmbitwes/menu/controller/AuthController.java`
- Modify: `controller/src/main/java/com/warmbitwes/menu/controller/DishController.java`
- Modify: `controller/src/main/java/com/warmbitwes/menu/controller/IngredientController.java`
- Create: `controller/src/main/java/com/warmbitwes/menu/controller/AdminUserController.java`
- Create: `controller/src/main/java/com/warmbitwes/menu/controller/AdminRoleController.java`
- Create: `controller/src/main/java/com/warmbitwes/menu/controller/AdminPermissionController.java`
- Create: `controller/src/main/java/com/warmbitwes/menu/controller/AdminScopeController.java`
- Create: `controller/src/main/java/com/warmbitwes/menu/dto/admin/*.java`
- Create: `common/src/main/java/com/warmbitwes/menu/security/*.java`
- Create: `service/src/main/java/com/warmbitwes/menu/service/AuthService.java`
- Create: `service/src/main/java/com/warmbitwes/menu/service/AdminUserService.java`
- Create: `service/src/main/java/com/warmbitwes/menu/service/AdminRoleService.java`
- Create: `service/src/main/java/com/warmbitwes/menu/service/AdminPermissionService.java`
- Create: `mapper/src/main/java/com/warmbitwes/menu/entity/AppUser.java`
- Create: `mapper/src/main/java/com/warmbitwes/menu/entity/Role.java`
- Create: `mapper/src/main/java/com/warmbitwes/menu/entity/PermissionPoint.java`
- Create: `mapper/src/main/java/com/warmbitwes/menu/entity/RolePermission.java`
- Create: `mapper/src/main/java/com/warmbitwes/menu/entity/RoleScope.java`
- Create: `mapper/src/main/java/com/warmbitwes/menu/mapper/AppUserMapper.java`
- Create: `mapper/src/main/java/com/warmbitwes/menu/mapper/RoleMapper.java`
- Create: `mapper/src/main/java/com/warmbitwes/menu/mapper/PermissionPointMapper.java`
- Create: `mapper/src/main/resources/mapper/AppUserMapper.xml`
- Create: `mapper/src/main/resources/mapper/RoleMapper.xml`
- Create: `mapper/src/main/resources/mapper/PermissionPointMapper.xml`
- Modify: `docs/db_init.sql`
- Test: `boot/src/test/java/com/warmbitwes/menu/auth/*.java`
- Test: `boot/src/test/java/com/warmbitwes/menu/controller/*.java`

### Task 1: 把登录从随机 Token 升级为真实鉴权

**Files:**
- Modify: `controller/src/main/java/com/warmbitwes/menu/controller/AuthController.java`
- Create: `service/src/main/java/com/warmbitwes/menu/service/AuthService.java`
- Create: `common/src/main/java/com/warmbitwes/menu/security/LoginUser.java`
- Create: `common/src/main/java/com/warmbitwes/menu/security/TokenService.java`
- Create: `controller/src/test/java/com/warmbitwes/menu/controller/AuthControllerTest.java`

- [ ] **Step 1: 写登录成功/失败的控制器测试**

```java
@WebMvcTest(AuthController.class)
class AuthControllerTest {

    @Autowired
    private MockMvc mockMvc;

    @MockBean
    private AuthService authService;

    @Test
    void login_should_return_token_when_credentials_valid() throws Exception {
        LoginResp resp = new LoginResp("access-token", "refresh-token", 7200);
        given(authService.login("admin", "123456", "admin")).willReturn(resp);

        mockMvc.perform(post("/api/auth/login")
                .contentType(MediaType.APPLICATION_JSON)
                .content("""
                    {"username":"admin","password":"123456","clientType":"admin"}
                    """))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.code").value(0))
            .andExpect(jsonPath("$.data.accessToken").value("access-token"));
    }
}
```

- [ ] **Step 2: 运行登录测试，确认当前实现无法通过**

Run: `./mvnw -pl controller -Dtest=AuthControllerTest test`

Expected: FAIL，原因是 `AuthController` 仍直接生成随机 UUID，且没有 `AuthService`

- [ ] **Step 3: 实现最小可用的 AuthService 与控制器改造**

```java
@RestController
@RequestMapping("/api/auth")
public class AuthController {

    private final AuthService authService;

    public AuthController(AuthService authService) {
        this.authService = authService;
    }

    @PostMapping("/login")
    public ApiResponse<LoginResp> login(@RequestBody @Valid LoginReq req) {
        return ApiResponse.success(
            authService.login(req.getUsername(), req.getPassword(), req.getClientType())
        );
    }
}
```

```java
@Service
public class AuthService {

    public LoginResp login(String username, String password, String clientType) {
        // 这里后续再接 mapper 校验账号、状态、端范围
        if (!"admin".equals(username) || !"123456".equals(password)) {
            throw new BizException(10001, "账号或密码错误");
        }
        return new LoginResp("access-token", "refresh-token", 7200);
    }
}
```

- [ ] **Step 4: 再次运行登录测试**

Run: `./mvnw -pl controller -Dtest=AuthControllerTest test`

Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add controller/src/main/java/com/warmbitwes/menu/controller/AuthController.java \
  service/src/main/java/com/warmbitwes/menu/service/AuthService.java \
  common/src/main/java/com/warmbitwes/menu/security/LoginUser.java \
  common/src/main/java/com/warmbitwes/menu/security/TokenService.java \
  controller/src/test/java/com/warmbitwes/menu/controller/AuthControllerTest.java
git commit -m "feat: add real auth login service"
```

### Task 2: 落库账号、角色、权限点、端范围数据模型

**Files:**
- Modify: `docs/db_init.sql`
- Create: `mapper/src/main/java/com/warmbitwes/menu/entity/PermissionPoint.java`
- Create: `mapper/src/main/java/com/warmbitwes/menu/entity/RolePermission.java`
- Create: `mapper/src/main/java/com/warmbitwes/menu/entity/RoleScope.java`
- Create: `mapper/src/main/java/com/warmbitwes/menu/mapper/PermissionPointMapper.java`
- Create: `mapper/src/main/resources/mapper/PermissionPointMapper.xml`
- Test: `mapper/src/test/java/com/warmbitwes/menu/mapper/PermissionPointMapperTest.java`

- [ ] **Step 1: 先写 mapper 层插入/查询权限点的失败测试**

```java
@MybatisTest
class PermissionPointMapperTest {

    @Autowired
    private PermissionPointMapper permissionPointMapper;

    @Test
    void should_insert_and_query_permission_code() {
        PermissionPoint point = new PermissionPoint();
        point.setCode("dish:create");
        point.setName("新增菜品");
        permissionPointMapper.insert(point);

        PermissionPoint loaded = permissionPointMapper.selectByCode("dish:create");
        assertEquals("新增菜品", loaded.getName());
    }
}
```

- [ ] **Step 2: 运行 mapper 测试，确认表/mapper 尚不存在**

Run: `./mvnw -pl mapper -Dtest=PermissionPointMapperTest test`

Expected: FAIL，原因是表结构和 mapper 未创建

- [ ] **Step 3: 在 `docs/db_init.sql` 中补齐最小 DDL**

```sql
CREATE TABLE IF NOT EXISTS permission_point (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  code VARCHAR(64) NOT NULL UNIQUE,
  name VARCHAR(128) NOT NULL,
  remark VARCHAR(512) DEFAULT NULL,
  created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updated_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3)
);

CREATE TABLE IF NOT EXISTS role_permission (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  role_id BIGINT NOT NULL,
  permission_id BIGINT NOT NULL,
  UNIQUE KEY uk_role_permission (role_id, permission_id)
);

CREATE TABLE IF NOT EXISTS role_scope (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  role_id BIGINT NOT NULL,
  client_type VARCHAR(32) NOT NULL,
  UNIQUE KEY uk_role_scope (role_id, client_type)
);
```

- [ ] **Step 4: 实现 entity / mapper / xml**

```java
public interface PermissionPointMapper {
    int insert(PermissionPoint entity);
    PermissionPoint selectByCode(@Param("code") String code);
    List<PermissionPoint> selectAll();
}
```

```xml
<select id="selectByCode" resultType="com.warmbitwes.menu.entity.PermissionPoint">
  select id, code, name, remark, created_at, updated_at
  from permission_point
  where code = #{code}
</select>
```

- [ ] **Step 5: 运行 mapper 测试**

Run: `./mvnw -pl mapper -Dtest=PermissionPointMapperTest test`

Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add docs/db_init.sql \
  mapper/src/main/java/com/warmbitwes/menu/entity/PermissionPoint.java \
  mapper/src/main/java/com/warmbitwes/menu/entity/RolePermission.java \
  mapper/src/main/java/com/warmbitwes/menu/entity/RoleScope.java \
  mapper/src/main/java/com/warmbitwes/menu/mapper/PermissionPointMapper.java \
  mapper/src/main/resources/mapper/PermissionPointMapper.xml \
  mapper/src/test/java/com/warmbitwes/menu/mapper/PermissionPointMapperTest.java
git commit -m "feat: add auth permission data model"
```

### Task 3: 增加受保护请求拦截与 API 权限校验

**Files:**
- Create: `common/src/main/java/com/warmbitwes/menu/security/AuthInterceptor.java`
- Create: `common/src/main/java/com/warmbitwes/menu/security/RequirePermission.java`
- Modify: `boot/src/main/java/com/warmbitwes/menu/MenuApplication.java`
- Create: `boot/src/main/java/com/warmbitwes/menu/config/WebMvcConfig.java`
- Test: `boot/src/test/java/com/warmbitwes/menu/auth/AuthInterceptorTest.java`

- [ ] **Step 1: 写越权请求被拒绝的失败测试**

```java
@SpringBootTest
@AutoConfigureMockMvc
class AuthInterceptorTest {

    @Autowired
    private MockMvc mockMvc;

    @Test
    void should_reject_request_without_permission() throws Exception {
        mockMvc.perform(post("/api/admin/users")
                .header("Authorization", "Bearer no-dish-create"))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.code").value(10003));
    }
}
```

- [ ] **Step 2: 运行测试，确认当前没有鉴权拦截**

Run: `./mvnw -pl boot -Dtest=AuthInterceptorTest test`

Expected: FAIL，因为当前受保护接口不存在或未拦截

- [ ] **Step 3: 实现拦截器 + 注解**

```java
@Target({ElementType.METHOD})
@Retention(RetentionPolicy.RUNTIME)
public @interface RequirePermission {
    String value();
}
```

```java
public class AuthInterceptor implements HandlerInterceptor {
    @Override
    public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) {
        // 解析 token，校验角色、端范围、权限点
        return true;
    }
}
```

- [ ] **Step 4: 在管理端 API 上打权限点注解**

```java
@PostMapping("/api/admin/users")
@RequirePermission("auth-user:create")
public ApiResponse<IdResp> create(@RequestBody @Valid AdminUserCreateReq req) {
    return ApiResponse.success(adminUserService.create(req));
}
```

- [ ] **Step 5: 跑 boot 鉴权测试**

Run: `./mvnw -pl boot -Dtest=AuthInterceptorTest test`

Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add common/src/main/java/com/warmbitwes/menu/security/AuthInterceptor.java \
  common/src/main/java/com/warmbitwes/menu/security/RequirePermission.java \
  boot/src/main/java/com/warmbitwes/menu/config/WebMvcConfig.java \
  boot/src/test/java/com/warmbitwes/menu/auth/AuthInterceptorTest.java
git commit -m "feat: enforce api permission checks"
```

### Task 4: 交付管理后台所需的账号/角色/权限/端范围 API

**Files:**
- Create: `controller/src/main/java/com/warmbitwes/menu/controller/AdminUserController.java`
- Create: `controller/src/main/java/com/warmbitwes/menu/controller/AdminRoleController.java`
- Create: `controller/src/main/java/com/warmbitwes/menu/controller/AdminPermissionController.java`
- Create: `controller/src/main/java/com/warmbitwes/menu/controller/AdminScopeController.java`
- Create: `controller/src/main/java/com/warmbitwes/menu/dto/admin/AdminUserCreateReq.java`
- Create: `controller/src/main/java/com/warmbitwes/menu/dto/admin/AdminUserUpdateReq.java`
- Create: `controller/src/main/java/com/warmbitwes/menu/dto/admin/AdminRoleCreateReq.java`
- Create: `controller/src/main/java/com/warmbitwes/menu/dto/admin/RolePermissionBindReq.java`
- Create: `controller/src/main/java/com/warmbitwes/menu/dto/admin/RoleScopeUpdateReq.java`
- Test: `boot/src/test/java/com/warmbitwes/menu/controller/AdminUserControllerTest.java`

- [ ] **Step 1: 写账号管理列表与新增接口测试**

```java
@WebMvcTest(AdminUserController.class)
class AdminUserControllerTest {

    @Autowired
    private MockMvc mockMvc;

    @MockBean
    private AdminUserService adminUserService;

    @Test
    void should_create_user() throws Exception {
        given(adminUserService.create(any())).willReturn(new IdResp(1L));

        mockMvc.perform(post("/api/admin/users")
                .contentType(MediaType.APPLICATION_JSON)
                .content("""
                    {"username":"ops","nickname":"运营","phone":"13800000000","email":"ops@example.com","password":"123456","roleIds":[1]}
                    """))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.code").value(0))
            .andExpect(jsonPath("$.data.id").value(1L));
    }
}
```

- [ ] **Step 2: 运行测试，确认 controller 不存在**

Run: `./mvnw -pl boot -Dtest=AdminUserControllerTest test`

Expected: FAIL

- [ ] **Step 3: 实现四组管理端 controller**

```java
@RestController
@RequestMapping("/api/admin/users")
public class AdminUserController {

    @GetMapping
    @RequirePermission("auth-user:list")
    public ApiResponse<List<AdminUserVO>> list() { ... }

    @PostMapping
    @RequirePermission("auth-user:create")
    public ApiResponse<IdResp> create(@RequestBody @Valid AdminUserCreateReq req) { ... }
}
```

```java
@RestController
@RequestMapping("/api/admin/roles")
public class AdminRoleController {
    @PutMapping("/{id}/permissions")
    @RequirePermission("auth-role:grant")
    public ApiResponse<Void> bindPermissions(...) { ... }
}
```

- [ ] **Step 4: 补齐 service 与 mapper 调用**

```java
public class AdminUserCreateReq {
    @NotBlank private String username;
    private String nickname;
    private String phone;
    private String email;
    @NotBlank private String password;
    @NotEmpty private List<Long> roleIds;
}
```

- [ ] **Step 5: 跑管理端 controller 测试**

Run: `./mvnw -pl boot -Dtest=AdminUserControllerTest test`

Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add controller/src/main/java/com/warmbitwes/menu/controller/AdminUserController.java \
  controller/src/main/java/com/warmbitwes/menu/controller/AdminRoleController.java \
  controller/src/main/java/com/warmbitwes/menu/controller/AdminPermissionController.java \
  controller/src/main/java/com/warmbitwes/menu/controller/AdminScopeController.java \
  controller/src/main/java/com/warmbitwes/menu/dto/admin \
  service/src/main/java/com/warmbitwes/menu/service/AdminUserService.java \
  service/src/main/java/com/warmbitwes/menu/service/AdminRoleService.java \
  service/src/main/java/com/warmbitwes/menu/service/AdminPermissionService.java \
  boot/src/test/java/com/warmbitwes/menu/controller/AdminUserControllerTest.java
git commit -m "feat: add admin auth management apis"
```

### Task 5: 对齐菜品/食材接口以满足后台与小程序联调

**Files:**
- Modify: `controller/src/main/java/com/warmbitwes/menu/controller/DishController.java`
- Modify: `controller/src/main/java/com/warmbitwes/menu/controller/IngredientController.java`
- Modify: `service/src/main/java/com/warmbitwes/menu/service/DishService.java`
- Modify: `service/src/main/java/com/warmbitwes/menu/service/IngredientService.java`
- Test: `boot/src/test/java/com/warmbitwes/menu/controller/DishControllerTest.java`
- Test: `boot/src/test/java/com/warmbitwes/menu/controller/IngredientControllerTest.java`

- [ ] **Step 1: 写菜品列表筛选与状态更新测试**

```java
@Test
void should_update_dish_status() throws Exception {
    mockMvc.perform(put("/api/dishes/1/status")
            .contentType(MediaType.APPLICATION_JSON)
            .content("""{"status":1}"""))
        .andExpect(status().isOk())
        .andExpect(jsonPath("$.code").value(0));
}
```

- [ ] **Step 2: 写食材详情与创建测试**

```java
@Test
void should_create_ingredient() throws Exception {
    mockMvc.perform(post("/api/ingredients")
            .contentType(MediaType.APPLICATION_JSON)
            .content("""
                {"name":"鸡蛋","unit":"个","caloriesKcalPer100g":143}
                """))
        .andExpect(status().isOk())
        .andExpect(jsonPath("$.code").value(0));
}
```

- [ ] **Step 3: 运行这两组测试，记录当前缺口**

Run: `./mvnw -pl boot -Dtest=DishControllerTest,IngredientControllerTest test`

Expected: 若现有请求/响应字段不一致则 FAIL；否则至少补齐筛选参数与唯一性校验测试

- [ ] **Step 4: 最小改造控制器与服务**

```java
@GetMapping
public ApiResponse<List<Dish>> page(@RequestParam(required = false) String name,
                                    @RequestParam(required = false) Integer status,
                                    @RequestParam(defaultValue = "1") int pageNum,
                                    @RequestParam(defaultValue = "10") int pageSize) {
    return ApiResponse.success(dishService.listPage(name, status, pageNum, pageSize));
}
```

```java
public Long createAndReturnId(Ingredient ingredient) {
    if (existsByName(ingredient.getName())) {
        throw new BizException(10021, "食材名称已存在");
    }
    mapper.insert(ingredient);
    return ingredient.getId();
}
```

- [ ] **Step 5: 跑菜品/食材测试**

Run: `./mvnw -pl boot -Dtest=DishControllerTest,IngredientControllerTest test`

Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add controller/src/main/java/com/warmbitwes/menu/controller/DishController.java \
  controller/src/main/java/com/warmbitwes/menu/controller/IngredientController.java \
  service/src/main/java/com/warmbitwes/menu/service/DishService.java \
  service/src/main/java/com/warmbitwes/menu/service/IngredientService.java \
  boot/src/test/java/com/warmbitwes/menu/controller/DishControllerTest.java \
  boot/src/test/java/com/warmbitwes/menu/controller/IngredientControllerTest.java
git commit -m "feat: align dish and ingredient apis for batch1"
```

## Self-Review

- Spec coverage:
  - AUTH A1：Task 1、2、3、4 覆盖真实鉴权、权限点、端范围、越权拒绝
  - 管理后台需要的账号/角色/权限：Task 4 覆盖
  - 菜品/食材主数据：Task 5 覆盖
- Placeholder scan: 已去掉 TBD/TODO/“后续补”式步骤；每个任务都有文件、测试、命令、提交
- Type consistency: `AdminUserCreateReq`、`RoleScopeUpdateReq`、`RequirePermission`、`PermissionPointMapper` 在各任务中命名一致
